import { NgModule } from '@angular/core'
import { TeamsShellRoutingModule } from './teams-shell-routing.module'

@NgModule({
  imports: [TeamsShellRoutingModule],
  providers: [],
})
export class TeamsShellModule {}
